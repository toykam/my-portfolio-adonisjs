@layout('admin/layout/main_layout')

@section('body')
<div id="layoutSidenav_content">
    <main>
        <div class="container-fluid px-4">
            <h1 class="mt-4">Skills</h1>
            <ol class="breadcrumb mb-4">
                <li class="breadcrumb-item active">Skills</li>
            </ol>

            @if(flashMessages.has('msg', '').length > 0)
                <div class="alert alert-{{ flashMessages.get('flag') }} text-center">
                    {{ flashMessages.get('msg') }}
                </div>
            @endif

            <div class="card mb-4">
                <div class="card-header">
                    <i class="fas fa-table me-1"></i>
                    All Skills
                    <span class="float-left">
                        <a href="{{ route('admin_new_skill') }}" class="btn btn-primary btn-sm">add skill</a>
                    </span>
                </div>
                <div class="card-body">
                    <table id="datatablesSimple">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Image</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tfoot>
                            <tr>
                                <th>Name</th>
                                <th>Proficiency</th>
                                <th>Image</th>
                                <th>Actions</th>
                            </tr>
                        </tfoot>
                        <tbody>
                            @each(skill in skills)
                                <tr>
                                    <td>{{ skill.skill_name }}</td>
                                    <td>{{ skill.proficiency }}</td>
                                    <td>
                                        <img src="/images/skills/{{ skill.skill_image_url }}" alt="{{ skill.skill_id }}" srcset="" height="50px">
                                    </td>
                                    <td>
                                        <a class="btn btn-sm btn-warning" href="{{ route('admin_edit_skill', {skill_id: skill.skill_id}) }}">edit</a>
                                        <a class="btn btn-sm btn-danger" href="{{ route('admin_delete_skill', {skill_id: skill.skill_id}) }}">delete</a>
                                        <a class="btn btn-sm btn-primary" href="">view</a>
                                    </td>
                                </tr>
                              
                            @endeach
                            
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        
    </main>
    
</div>
@endsection